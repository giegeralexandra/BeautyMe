<h1><%= @category.name %></h1>
<br>

<h2>Appointments</h2>
<% @category.appointments.each do |a| %>
    <p><%= link_to a.name.capitalize, category_appointment_path(a) %></p>
    <p><%= a.date_view + " " + a.start_time_view %></p>
<% end %>
<br>

<h2>Customers</h2>
<% @category.customers.uniq.each do |c| %> 
    <p><%= link_to c.full_name, category_customer_path(c) %></p>
<% end %>
<br>

<p><%= button_to "New Appointment", new_category_appointment_path(@category) %></p>
<p><%= button_to "Edit Category", edit_category_path(@category) %></p>
<p><%= button_to "Delete Category", category_path(@category), method: :delete %></p>
